.headers on

WITH CHANGES AS(
  SELECT
    DATE,
    COLUMNS AS NEW_COLUMNS,
    LAG(COLUMNS) OVER(ORDER BY DATE) AS PAST_COLUMNS
  FROM
    EXOPLANETS_COLUMNS
)

SELECT
    *
FROM
    CHANGES
WHERE
    NEW_COLUMNS != PAST_COLUMNS
ORDER BY
    DATE ASC
